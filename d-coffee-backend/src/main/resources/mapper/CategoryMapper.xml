<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dcoffee.mapper.CategoryMapper">
    <select id="findAdminCategories" resultType="com.dcoffee.vo.CategoryView">
        SELECT id, category_code AS code, name, icon_url, sort, status
        FROM dc_category WHERE deleted = 0 ORDER BY sort, id
    </select>
    <select id="categoryExists" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM dc_category WHERE id = #{id} AND deleted = 0)
    </select>
    <select id="codeExists" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM dc_category WHERE category_code = #{code} AND deleted = 0
            <if test="excludeId != null">AND id &lt;&gt; #{excludeId}</if>)
    </select>
    <insert id="insertCategory">
        INSERT INTO dc_category (category_code, name, icon_url, sort, status, deleted)
        VALUES (#{category.code}, #{category.name}, #{category.iconUrl}, #{category.sort}, 'ACTIVE', 0)
    </insert>
    <update id="updateCategory">
        UPDATE dc_category SET category_code = #{category.code}, name = #{category.name},
            icon_url = #{category.iconUrl}, sort = #{category.sort}
        WHERE id = #{id} AND deleted = 0
    </update>
    <update id="updateStatus">
        UPDATE dc_category SET status = #{status} WHERE id = #{id} AND deleted = 0
    </update>
</mapper>
